{
  "framework": null,
  "buildCommand": "mkdir -p /tmp/hugo && curl -L https://github.com/gohugoio/hugo/releases/download/v0.145.0/hugo_extended_0.145.0_linux-amd64.tar.gz | tar -xz -C /tmp/hugo && export PATH=/tmp/hugo:$PATH && hugo version && hugo --environment production",
  "outputDirectory": "public"
} 